let arrayAnyType  = [1,33, 42,22,99]
let arrayAnyType2 = ["r", "u", "h", "f"]
let arr = []
arr. push (1)
arr. push (21)
arr.push (33)
arr. push (2)
console. log (arr)

arr.pop(); //removes last console. log (arr)
arr. shift(); //removes first console. log(arr)

arr.unshift( 45) //adds element at begining console. log(arr)

console. log(arr.length) //finds length

console. log(arr.includes(21)) // returns true if present

console. Log(arr.index0f(21)) //index of the of the value

console. log(arr.slice(1,3)) // substring

console. log(Math.max(...arr)) //find the max console. log(Math.min(...arr)) // find the min

console. log(arrayAnyType.reverse())

arr. sort ((a, b) => a - b)// Ascending
arr. sort( (a, b) => b - a) // Descending